import React from 'react';

type CalendarProps = {
  date: number;
};

const Calendar: React.FC<CalendarProps> = () => {
  return (
    <div>
      <h2>Calendar</h2>
    </div>
  );
};

export default Calendar;
